# Dataset — too large for git
/Data

# YOLO training runs — checkpoints too large, but weights will go via LFS
# /runs

# Temp files
text.txt
*.cache
__pycache__/
*.pyc
.ipynb_checkpoints/

# MLflow logs
mlruns/