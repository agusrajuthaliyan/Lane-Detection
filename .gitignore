# Dataset — too large for git
/Data

# YOLO training runs — too large
/runs

# Model weights — too large for git (push separately or use Git LFS)
*.pt

# Temp files
text.txt
*.cache
__pycache__/
*.pyc
.ipynb_checkpoints/

# MLflow logs
mlruns/